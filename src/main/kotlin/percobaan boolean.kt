fun main(args: Array<String>) {
    var varBool : Boolean = true
    if(varBool)
        println("varBool bernilai " + varBool)
    var varInt : Int = 2
    //Jika menggunakan if(varInt) maka program akan eror karena terdapat 2 kondisi if
    // if(varInt)
    println("varInt bernilai " + varInt)
}